#pragma once

/**
 * Класс калькулятора.
 * Выполняет простые арифметические операции.
 */
class Calculator {
public:
    /// Складывает два числа.
    int sum(int a, int b);
};

/// Умножает два числа.
int multiply(int a, int b);
